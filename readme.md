First set the "dirpath" to the path that OSX save screen capture to,

Then run:

`python replaceClip.py`

will get the url by automatically uploading that image to leancloud and copy that url to system clipboard.
